name: Job Scraper V3

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test-v3:
    runs-on: ubuntu-latest

    steps:
      - name: V3 registration test
        run: echo "Job Scraper V3 is registered correctly"
